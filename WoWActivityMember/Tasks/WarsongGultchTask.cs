namespace WoWActivityMember.Tasks.SharedTasks
{
    public class WarsongGultchTask(IClassContainer container, Stack<IBotTask> botTasks) : BotTask(container, botTasks, TaskType.Ordinary), IBotTask
    {
        public void Update()
        {

        }
    }
}
